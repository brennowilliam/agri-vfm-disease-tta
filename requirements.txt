# Paper 1 — core deps. On Kaggle/Colab most of these are preinstalled;
# `pip install -q timm open_clip_torch` is usually all you need on top.
torch>=2.1
torchvision>=0.16
timm>=1.0.9
open_clip_torch>=2.24
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
Pillow>=10.0
tqdm>=4.66
